<% provide(:title, @user.name) %>

<div class ="row">
	<aside class = "span4">
		<section>
	<h1>
		<%= gravatar_for @user, size: 100 %> <!--gravatar for user in heading -->
		<%= @user.name %>
	</h1>
		</section>
	</aside>
</div>
